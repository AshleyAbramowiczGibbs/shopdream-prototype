# When user selects the item_tag from style index or show it takes them to this page to show all item tags for a particular style id
  # name of tag
  # image of style (based on id)
  # potential purchases associated with it (images)

json.partial! "item_tag.json.jbuilder", item_tag: @item_tag